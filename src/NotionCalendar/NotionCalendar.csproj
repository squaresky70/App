<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net8.0-windows10.0.19041.0</TargetFramework>
    <TargetPlatformMinVersion>10.0.17763.0</TargetPlatformMinVersion>
    <RootNamespace>NotionCalendar</RootNamespace>
    <AssemblyName>NotionCalendar</AssemblyName>
    <ApplicationManifest>app.manifest</ApplicationManifest>

    <!-- ARM64 PC 라면 win-arm64 로 바꾸세요. -->
    <RuntimeIdentifier>win-x64</RuntimeIdentifier>

    <UseWinUI>true</UseWinUI>
    <!-- MSIX 로 패키징하지는 않지만, PRI(리소스) 생성 태스크가 이 도구를 필요로 한다. -->
    <EnableMsixTooling>true</EnableMsixTooling>

    <!-- 패키징(MSIX) 없이 그냥 exe 로 실행 -->
    <WindowsPackageType>None</WindowsPackageType>
    <!-- Windows App Runtime 을 따로 설치하지 않아도 되도록 자체 포함 -->
    <WindowsAppSDKSelfContained>true</WindowsAppSDKSelfContained>
    <SelfContained>true</SelfContained>

    <Nullable>enable</Nullable>
    <LangVersion>latest</LangVersion>
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.WindowsAppSDK" Version="1.6.*" />
    <PackageReference Include="Microsoft.Windows.SDK.BuildTools" Version="10.0.26100.1742" />
  </ItemGroup>

</Project>
